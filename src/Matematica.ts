
export function somar (n1: number, n2: number): number {
    return(n1 + n2);
}

export function sub (n1: number, n2: number): number {
    return(n1 - n2);
}

export function mult (n1: number, n2: number): number {
    return(n1 * n2);
}